/*
    Just mains
*/
#include <iostream>
#include <vector>
// #include "rcoins.h"
// #include "pcells.h"
#include "subsets.h"
using std::cout;
using std::endl;
using std::cin;
using std::vector;

int main()
{
    cout << "Hola Hot Stuff" << endl;
    int x = 5;
    while (x-- > 0)
    {
        if (x == 2) continue;
        cout << x << endl;
        
    }

    // int x = 5;
    // while (x-- > 0)
    // {
    //     vector<int> set;
    //     set.push_back(5);
    //     cout << x;
    // }
    
    // Solution s = Solution();
    // s.silly(vector<int>());
    // vector<int> t;
    // cout << "size: " << t.size() << endl;

    // vector<int> v = {1, 2, 3};
    // Solution s = Solution();
    // vector<vector<int>> res = s.subsets(v);
    // // s.silly(days);
    // for (auto it = res.begin(); it != res.end(); ++it)
    // {
    //     vector<int> cur = *it;
    //     for (auto i = cur.begin(); i != cur.end(); ++i=)
    //     {
    //         cout << *i << ", ";
    //     }
    //     cout << endl;
    // }
    // cout << "all done boss" << endl;




    // CELL BLOCKS
    // vector<int> temp{0, 1, 0, 1, 1, 0, 0, 1};
    // vector<int> temp1{1, 0, 0, 1, 0, 0, 1, 0}; 
    // vector<int> &vref = temp;
    // int days = 1000000000;
    // int days7 = 7;



//    coins!!
//    cout << "Hola Hot Stuff, enter a number" << endl;
//    int coins = 0;
//    cin >> coins;
//    cout << coins << endl;
//    Solution s = Solution();
//    coins = s.arrangeCoins(coins);
//    cout << "You can build: " << coins << " steps" << endl;
}